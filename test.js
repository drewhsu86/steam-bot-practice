const responses = require('./resController')

// console.log(responses.stringConditions(
//   'Is it good or bad?', ['is', 'or'], ['is it good or bad?', 'are it gud or bed'], ['ito']
// ))

console.log(responses.stringResponse('what are you doing'))